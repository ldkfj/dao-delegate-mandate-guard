[project]
name = "dao-delegate-mandate-guard"
version = "0.1.0"
description = "DAO Delegate Mandate Guard Intelligent Contract"
requires-python = ">=3.12"
dependencies = [
    "genlayer-py==0.16.3",
    "genlayer-test==0.29.2",
    "genvm-linter==0.11.0",
    "pytest==9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests/direct"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
